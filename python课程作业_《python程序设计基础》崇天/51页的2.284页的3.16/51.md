##### 作业题目是51页的2.284页的3.16

老师啊是我真的没有看懂这到底是布置的那页的作业和哪一题

找了相应的页面，没对上号

######我就把这两页抄一下吧

***
51页
##### 画笔控制函数

    turtle.penup()和turtle.pendown函数
    
    turtle.penup()
    turtle.pendown()

turtle中的画笔可以通过一组函数来控制。

    turtle.pensize()函数
    turtle.pensize(25)

turtle.pensize()函数用来设置画笔尺寸    turtle.pensize(width)


    turtle.pencolor("purple")

画笔颜色

***

84页

###

str.isspace
>当str所有字符都是空格,返回True，否则False

>str.split(sep=None,maxsplit=-1)
返回一个列表，有str根据sep被分割的部分构成

str.join(iterable)
>返回一个新的字符串，由组合数据类型iterable变量的每个元素组成，元素间用str分隔



